#!/usr/bin/env bash

files=$(find . -maxdepth 1 -type f -name '.*')

# Source all files in directory
for file in $files ; do
  if [ -f "$file" ]  && [ "${file}" != "./.init" ]; then
    echo $file
    . "$file"
  fi
done